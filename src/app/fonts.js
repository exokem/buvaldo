/* eslint-disable camelcase */
import {Passion_One, Inter, Permanent_Marker} from "next/font/google"

export const passionOne = Passion_One({subsets: ["latin"], display: 'swap', weight: '700'})
export const inter = Inter({subsets: ['latin'], display: 'swap'})
export const handwriting = Permanent_Marker({subsets: ['latin'], display: 'swap', weight: '400'})
