---
interface Props {
	id?: string,
	class?: string,
}

const {
	id = '',
	class: className = '',
} = Astro.props
---

<span id={id} class:list={["material-symbols-rounded", "icon", className]}>
	<slot/>
</span>
